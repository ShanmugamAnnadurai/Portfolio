import { Component } from '@angular/core';

@Component({
  selector: 'app-projects',
  templateUrl: './projects.component.html',
  styleUrl: './projects.component.css',
})
export class ProjectsComponent {
  projectOne() {
    // this has to navigate to github project one
  }
  projectTwo() {
    // this has to navigate to github project one
  }

  projectThree() {
    // this has to navigate to github project one
  }
}
